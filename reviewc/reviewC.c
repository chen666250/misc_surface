#include<stdio.h>
#include<float.h>
int main()
{
	/*hello->C long time no see*/
 printf("long time no see\n");
 printf("long time no see int size is %lu \n",sizeof(int));
 printf("float max is %E \n",FLT_MAX);
 printf("float min is %E \n",FLT_MIN);

 return 0;
}
